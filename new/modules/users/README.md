# users

Configuration per user.

> [!NOTE]
> Users may have host specific configurations, look at
> `hosts/<host>/users/<user>.nix` for additional configuration.

## flake-parts.nix

For a user to be available as aspect you have to define it in `flake-parts.nix`.
