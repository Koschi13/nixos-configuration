# flake-parts

This directory contains direct configurations for `flake-parts`.
This will mostly consists of generic definition of `aspect`.

Individual modules might contain a `flake-parts.nix` file to extend the inputs
if needed, or provide additional configuration which is needed for that
module to work.
