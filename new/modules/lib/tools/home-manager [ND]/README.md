# home-manager

Contains the required code to set up `Home-Manager` as module.
This is also the place to configure `Home-Manager` itself.

It will be automatically enabled for all `nixos` and `darwin` classes.
